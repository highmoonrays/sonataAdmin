{% extends 'base.html.twig' %}
{% block navbar %}
    <div class="topnav">
        <a class="active" href="{{path('uploadFile')}}">Upload file</a>
        <a class="active" href="{{path('createNewProduct')}}">Fill form</a>
        <a class="active" href="{{ path('products') }}">All products</a>
        <a class="active" href="{{ path('changePassword') }}">Change Password</a>
        <a class="active" href={{ path('sonata_admin_dashboard') }}>Log as Admin</a>
        <a class="active" href={{ path('app_logout') }}>Log out</a>
    </div>
{% endblock %}
